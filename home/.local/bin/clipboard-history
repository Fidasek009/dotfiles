#!/bin/sh
set -eu

selection=$(cliphist list | fuzzel --dmenu --config "$HOME/.config/fuzzel/clipboard.ini" --prompt "Clipboard> ") || exit 0
[ -n "$selection" ] || exit 0

printf '%s' "$selection" | cliphist decode | wl-copy
hyprctl dispatch sendshortcut CTRL,V,activewindow
